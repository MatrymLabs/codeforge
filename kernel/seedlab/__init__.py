"""seedlab -- the engineering-environment prototype (the recentered Seed).

The directive "THE SEED IS THE MUD" recenters a Seed from a game-data pack to a persistent,
MUD-based software-engineering environment: a workshop that connects a project source, models it,
builds it, tests it, and generates a target. This package is the FIRST, isolated slice of that
runtime -- deliberately separate from the game (kernel/world/), so the game Seed model is untouched
while the engineering Seed is proven from scratch. Status: PROTOTYPED (see docs/seed_platform/).
"""
